<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TopacAdemy_android"
        tools:targetApi="31">

        <activity
            android:name=".feature.weather.presentation.ui.WeatherDetailActivity"
            android:parentActivityName=".feature.weather.presentation.ui.WeatherActivity" />

        <activity
            android:name=".feature.weather.presentation.ui.WeatherActivity"
            android:exported="false" />

        <activity
            android:name=".feature.cars.presentation.ui.ListActivity"
            android:exported="false" />

        <activity
            android:name=".feature.calculator.presentation.ui.CalculatorActivity"
            android:exported="false" />

        <activity
            android:name=".feature.home.presentation.ui.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".feature.home.presentation.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>